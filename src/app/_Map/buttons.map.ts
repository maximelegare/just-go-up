import { BaseButton } from '@app/components/Buttons/Base'
import { UnderlineButton } from '@app/components/Buttons/Underline'

export const buttonsComponentsMap = {
  underline: UnderlineButton,
  link: UnderlineButton,
  base: BaseButton,
}
